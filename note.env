// LandingPage.tsx (Snippet)

// ...

// ----------------------------------------------------
// REMOVE THE OLD GradientText WRAPPER AND INNER H1/P
// ----------------------------------------------------

// Title (Now correctly uses GradientText)
<GradientText
  colors={["#40ffaa", "#4079ff", "#40ffaa"]} // Colors from GradientTextWrapper
  animationSpeed={3}
  showBorder={false}
  className="custom-class"
>
  <motion.h1 // Apply Framer Motion to the title element
    initial={{ opacity: 0, y: 60 }}
    animate={{ opacity: 1, y: 0 }}
    transition={{ duration: 0.8 }}
    className="text-6xl md:text-8xl font-extrabold uppercase tracking-widest drop-shadow-lg"
    // REMOVED ALL custom inline style props (color, textShadow, etc.)
    // These were conflicting with the gradient logic in GradientText.
  >
    ZEPHVION
  </motion.h1>
</GradientText>

{/* Subtitle (Moved OUTSIDE GradientText for better contrast) */}
<motion.p
  initial={{ opacity: 0, y: 30 }}
  animate={{ opacity: 1, y: 0 }}
  transition={{ duration: 0.9, delay: 0.2 }}
  className="text-base md:text-xl font-light tracking-[0.3em] text-white"
  style={{
    // Set text to pure white (or your desired contrast color)
    color: "#FFFFFF",
    // Soft white glow for subtitle
    textShadow: "0 0 8px rgba(255, 255, 255, 0.4)",
  }}
>
  TRUST TODAY TRANSFORM TOMORROW
</motion.p>

// ...